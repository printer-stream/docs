# Top-level runtime dependencies for the FTS5-only MCP server + indexer.
# Search is pure SQLite FTS5 -- no embeddings, so the heavy ML stack
# (torch / transformers / sentence-transformers / sqlite-vec) is gone.
# The fully pinned transitive closure lives in requirements.txt.
mcp>=1.2.0
pydantic-settings>=2.0.0
uvicorn>=0.30.0
