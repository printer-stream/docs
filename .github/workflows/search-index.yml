name: Render search index

on:
  push:
    branches:
      - main
    paths:
      - 'md/**'
      - 'mcp-server/indexer.py'
      - 'mcp-server/corpus.py'
      - 'mcp-server/summarizer.py'
      - 'mcp-server/config.py'
  workflow_dispatch:

permissions:
  contents: write
  packages: read

# Share the serialization group so this never pushes to main at the same time
# as the PDF conversion / render workflows.
concurrency:
  group: pdf-artifacts-${{ github.ref }}
  cancel-in-progress: false

jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Read index image version
        id: index_image_version
        shell: bash
        run: >-
          grep __version__ mcp-index-image/version.py | cut -d "=" -f 2 | tr -d "'\" " > .index-image-version
          && echo "version=$(cat .index-image-version)" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull index image
        run: >-
          docker pull
          ghcr.io/${{ github.repository }}/mcp-index-image:${{ steps.index_image_version.outputs.version }}

      - name: Build index
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -e DOCS_REPO_ROOT=/workspace \
            -e DOCS_INDEX_PATH=/workspace/index/specs.db \
            ghcr.io/${{ github.repository }}/mcp-index-image:${{ steps.index_image_version.outputs.version }} \
            python /workspace/mcp-server/indexer.py

      - name: Commit index if changed
        run: |
          if [ -n "$(git status --porcelain index/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add index/
            git commit -m "[Auto] Rebuild search index"
            git pull --rebase origin main
            git push
          else
            echo "Index unchanged."
          fi
