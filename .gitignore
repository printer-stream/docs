# Python
__pycache__/
*.py[cod]
.venv/
.venv-*/
venv/
env/

# Local model / HF caches
.cache/
*.egg-info/

# OS metadata and local secrets — never commit these.
.DS_Store
**/.env

# Note: index/ is intentionally tracked — it's built by CI and loaded by the
# MCP server at boot.
